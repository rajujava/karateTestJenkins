package feature;

import org.junit.Test;


public class TestRunner{
    
@Test
public void testParallel() {
	 
		/*
		 * System.setProperty("mock.env", "karateTesting"); // ensure reset if other
		 * tests (e.g. mock) had set env in CI Results results =
		 * Runner.parallel(getClass(), 5);
		 * TestParallel.generateReport(results.getReportDir());
		 * assertTrue(results.getErrorMessages(), results.getFailCount() == 0);} public
		 * static void generateReport(String karateOutputPath) { Collection<File>
		 * jsonFiles = FileUtils.listFiles(new File(karateOutputPath), new String[]
		 * {"json"}, true); List<String> jsonPaths = new ArrayList(jsonFiles.size());
		 * jsonFiles.forEach(file -> jsonPaths.add(file.getAbsolutePath()));
		 * Configuration config = new Configuration(new File("target"),
		 * "karateTesting"); ReportBuilder reportBuilder = new ReportBuilder(jsonPaths,
		 * config); reportBuilder.generateReports();
		 */    }
}
